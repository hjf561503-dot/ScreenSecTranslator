# Changelog

## 2.0.4

- 删除模型可见的 `ZZX/XZZ` 术语占位符与 `QSEG` 批量分隔符，修复内部标记泄漏、英文翻成英文、多行粘连及空格标点丢失。
- 翻译任务改为每个 OCR 行独立请求、独立质量检查和独立坐标覆盖。
- 静态页面按小批次持续处理尚未翻译的 OCR 行，累计已有译文；只有全部候选行处理完成后才跳过未变化画面。
- 拒绝没有中文、等同英文原文、含内部标记或破坏 URL、IP、CVE、哈希、路径、工具名及安全缩写的输出。
- OCR 上限提高到 1800 像素、候选行提高到 100 行，缓存提高到 800 条，并扩充安全仪表盘与 SQL 注入状态术语。

## 2.0.3

- 修复译文层首次出现时通过删除并重新添加悬浮球来置顶的逻辑；三星 Android 16 上若重新添加失败会导致悬浮球永久消失或服务异常。现在悬浮球只会在服务真正停止时删除。
- 使用 ML Kit `RemoteModelManager.isModelDownloaded()` 显示设备中的真实模型状态；未下载时先完成下载和二次校验，再申请录屏授权并启动服务。
- 删除容易误解的“已启动：模型就绪后……”状态；服务启动成功时明确显示“离线模型已验证”。
- 新增 APK 内置及 GitHub 免费更新的网络安全术语 TSV，不需要 API，也不会上传截图或 OCR 文字。
- 保护 Dirb、DirBuster、Nmap、Burp Suite、Metasploit 等工具名及 CamelCase 产品名，避免被音译成人名或普通词。
- 翻译缓存改为同一 App 进程内跨服务重启复用，减少重新启用悬浮窗后的重复翻译。
- 译文覆盖层的窗口创建现在有故障隔离：即使系统拒绝新增全屏覆盖层，也只记录错误并保留悬浮球，不再让服务随未捕获异常退出。
- 日志新增悬浮球点击/长按事件，便于区分触摸、截帧、OCR 和覆盖层阶段。

## 2.0.2

- 根据三星 Tab S9+（Android 16）详细日志修复同尺寸 `onCapturedContentResize` 回调反馈环；旧版在约 90 分钟内重建捕获通道 9611 次，导致后续始终没有可读帧。
- 只有捕获宽、高或 density 真正变化时才重建 `ImageReader` 和 `VirtualDisplay`，同尺寸回调直接忽略。
- 取帧时不再隐藏悬浮球，点击后始终可见；只隐藏全屏译文层，避免 OCR 再次识别旧译文。
- 连续取帧超时采用 4–30 秒退避，首次才弹提示，避免红色感叹号和 Toast 疯狂循环。
- 日志版本号改为自动读取构建版本，后续日志可准确区分 APK。

## 2.0.1

- 删除悬浮球和译文层的 `FLAG_SECURE`，恢复系统正常截图，并避免部分设备的 MediaProjection 被安全覆盖层干扰。
- 改为监听 `ImageReader` 新帧事件、通过 `acquireLatestImage()` 捕获最新帧，并把首帧等待由约 0.6 秒延长到约 3 秒。
- 修正帧转换使用捕获图像自身尺寸与步幅，增强旋转和分辨率变化时的稳定性。
- OCR 长边限制为 1600 像素，未缓存文本按最多 12 行批量翻译；刷新范围改为 0.3–2.0 秒，默认 0.6 秒。
- 增加密码保护的详细 TXT 日志下载按钮（密码 `20121013`），记录取帧与翻译耗时但不记录屏幕文字或密钥。

## 2.0.0

- 将默认流程改为设备端实时 OCR、离线中英翻译与自动覆盖，不再依赖云端 API。
- 增加 0.7–3.0 秒刷新间隔、画面指纹跳帧、600 条 LRU 翻译缓存和覆盖层自隐藏。
- 增加网络安全本地术语表，并保护 URL、IP、CVE、哈希、路径、命令和常见缩写。
- 增加 ML Kit 离线模型预下载、运行状态和 Google Translate 归属标识。
- 将在线 AI 改为默认关闭的手动长按精译，增加 Gemini 免费层优先与 OpenAI 后备。
- 增加 Android 16 使用说明、隐私说明和免费层数据使用提示。
- 增加推送/PR/每周构建监控、CodeQL、依赖审查、Dependabot、CODEOWNERS 和 CI 密钥扫描。

## 1.0.0

- 首次实现 Android 悬浮球与单帧屏幕翻译。
- 增加 Android 14+ MediaProjection 前台服务支持。
- 增加自然中文与网络安全术语提示词。
- 增加 OpenAI 视觉输入、原始细节和严格结构化输出。
- 增加译文坐标覆盖、旋转重设、代理鉴权和速率限制。
- 增加自定义术语表、无存储请求与提示注入防护。
- 增加 Node 单元测试和 GitHub Actions APK 构建工作流。
